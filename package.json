{
  "name": "scriptorium",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @scriptorium/site-runtime && npm run dev --workspace scriptorium-project-docs-template",
    "dev:runtime": "npm run build --workspace @scriptorium/site-runtime",
    "dev:example": "npm run dev --workspace scriptorium-project-docs-template",
    "build": "npm run build --workspace @scriptorium/site-runtime",
    "build:example": "npm run build --workspace @scriptorium/site-runtime && npm run build --workspace scriptorium-project-docs-template",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @scriptorium/site-runtime",
    "validate": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/semver": "^7.7.1",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
