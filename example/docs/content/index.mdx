---
title: Overview
description: Shared docs base for one project with multiple published refs.
---

# Overview

This starter is built for projects that want:

- one docs site per project
- a shared shell across projects
- ref selection for `dev`, `release/*`, and tags

By default, this example exposes:

- the current `dev` branch by default
- matching `release/*` branches when they exist
- tags when they exist

## Contract

Your project repo provides:

- `docs/content/**`
- `docs/assets/**`
- `scriptorium.project.json`
